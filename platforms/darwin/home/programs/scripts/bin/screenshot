#!/usr/bin/env bash

FILE="$HOME/screen.png"

select=0

while getopts "s" opt; do
    case "$opt" in
        s) select=1 ;;
    esac
done

# make screenshot
if [ "$select" = 1 ]; then
    screencapture -xci "$FILE"
else
    screencapture -xc "$FILE"
fi
